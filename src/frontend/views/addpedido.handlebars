<div>
    <h1>Criar Pedido</h1>
    <form id="formaddpedido" action="#" method="post">
        <label for="selectcliente">Cliente:</label>
        <select id="selectcliente" name="cliente">
            {{#each clientes}}
                <option value="{{this.id}}">{{this.nome}}</option>
            {{/each}}
        </select>
    </form>
</div>
